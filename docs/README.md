# OpenMicroDuck Documentation

Choose a language / 选择语言：

- [English](en/README.md) — default repository language
- [简体中文](zh-CN/README.md) — parallel primary documentation language

English and Simplified Chinese are maintained as parallel documentation trees with the same topic hierarchy wherever practical. English is the default landing language, but the Chinese tree is not a shortened summary.

Future languages can be added as sibling directories such as `ja/`, `fr/`, `de/`, or `es/`.